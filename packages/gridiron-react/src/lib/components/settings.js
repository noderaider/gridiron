
export default function settings (pure) {
  const { React, defaults } = pure

  return pure (
    { displayName: 'Settings'
    , render() {
        return (
          <div>
          </div>
        )
      }
    }
  )
}
